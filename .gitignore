# === Build outputs ===
*.apk
*.aab
*.ap_
*.dex
*.class
*.jar
*.aar
*.war

# Keep the Gradle wrapper jar (needed by ./gradlew); excluded from *.jar above.
!gradle/wrapper/gradle-wrapper.jar

# === Build folders (Gradle, R, generated sources) ===
build/
*/build/
**/build/
**/bin/
**/gen/
.cxx/
.externalNativeBuild/

# === Gradle ===
.gradle/
gradle-app.setting
.gradletasknamecache
local.gradle

# === Kotlin / KAPT / KSP caches ===
.kotlin/
*.kotlin_module
*.kapt_metadata
*.kapt_stubs
*.tab
*.len
*.at

# === IDE — IntelliJ / Android Studio ===
.idea/
*.iml
*.iws
*.ipr
.navigation/
captures/
output.json

# === IDE — other ===
.vscode/
.fleet/
*.swp
*.swo
*.bak
*~

# === Local config / secrets ===
local.properties
keystore.properties
key.properties
*.jks
*.keystore
release.keystore
debug.keystore
*.pepk
*.aps

# Extensionless signing keys (e.g. keytool output with no .jks/.keystore suffix)
quotify_release_key
*_release_key
*.p12

# Firebase configs are project-specific — decide per repo whether to commit.
# Uncomment to keep them local-only:
# **/google-services.json
# **/GoogleService-Info.plist

# === OS junk ===
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
ehthumbs_vista.db

# === Logs / profiling / heap dumps ===
*.log
*.hprof
*.profraw
hs_err_pid*

# === Design / spec dumps left in repo root ===
/*.html

# === Tooling caches ===
.claude/

# === Cloudflare Worker (worker/) ===
node_modules/
.wrangler/
.dev.vars
# Приватний ключ сервісного акаунта Google (Play API + Firestore) — лише локально,
# у Cloudflare він живе як secret GOOGLE_SA_KEY.
worker/quotify-sa.json
*-sa.json
