package com.kovhan.core.models.widget

/** Horizontal alignment of the quote block on the widget. */
enum class WidgetTextAlign {
    START,
    CENTER,
    ;

    companion object {
        val DEFAULT = START
    }
}
