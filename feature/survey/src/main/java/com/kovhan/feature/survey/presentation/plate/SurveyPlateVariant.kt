package com.kovhan.feature.survey.presentation.plate

/**
 * How the survey entry plate sits on the profile screen:
 * - [CARD] — elevated card with a hairline border, aligned to page padding
 * - [GOLD] — soft olive fill, no border
 * - [GOLD_FULL] — the same fill bled to the screen edges
 */
enum class SurveyPlateVariant { CARD, GOLD, GOLD_FULL }
